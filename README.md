# command

```
$ yarn create react app template
$ cd template
$ yarn add -D hygen eslint prettier husky lint-staged typescript
$ yarn create @eslint/config
$ yarn husky:init
$ npx -p @storybook/cli sb init --type react
$ hygen comopnent new // npm i -g hygen する必要があるかも
$ yarn start
$ yarn storybook
$ git add .
$ git commit -m ":tada: init"
```
